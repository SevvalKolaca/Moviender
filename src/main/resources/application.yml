server:
  port: 8080

tmdb:
  api-key: 7d3c575703f680c059013f9eed65101d

spring:
  application:
    name: moviender

  datasource:
    url: jdbc:postgresql://localhost:5432/Moviender
    username: postgres
    password: 1234
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update  # Entity?lere g�re tablo yoksa olusturur, varsa sadece de?i?iklikleri uygular
      show-sql: true      # hibernate taraf?ndan olusturulan SQL komutlar?n?n konsola yaz?lmas?
      format_sql: true  # sorgunun konsolda daha okunaklı olmasını saglar